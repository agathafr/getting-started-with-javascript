console.log("Working with variables");

const age = 29;
const name = "Ricardo";
let year = 2000;

// don't do because not putting "const" raises the variable to a general scope, which makes traceabillity difficult
// month = "January"

